#!/usr/bin/expect -f

set timeout 20
set host "31.97.21.53"
set user "root"
set password "@Ahs221400#123"

spawn ssh $user@$host
expect {
    "yes/no" { send "yes\r"; exp_continue }
    "password:" { send "$password\r" }
}

expect "#"
send "cd ~/hercules-assist-insight\r"
expect "#"
send "docker compose logs backend\r"
expect "#"
send "exit\r"
expect eof
